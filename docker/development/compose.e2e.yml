services:
  database:
    container_name: documenso-e2e-database
    ports: !reset []

  inbucket:
    container_name: documenso-e2e-inbucket
    ports: !reset []

  redis:
    container_name: documenso-e2e-redis
    ports: !reset []

  minio:
    container_name: documenso-e2e-minio
    ports: !reset []

  gotenberg:
    container_name: documenso-e2e-gotenberg
    ports: !reset []
